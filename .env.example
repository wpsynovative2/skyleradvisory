# Google Apps Script web app URL (see google-apps-script/README.md).
# Deploy Code.gs as a web app, then paste the /exec URL here.
GOOGLE_SCRIPT_URL=

# Optional shared secret. Must match the TOKEN script property in Apps Script.
GOOGLE_SCRIPT_TOKEN=
